package math;

public class MathMethods {

    public static void main(String[] args) {
        int x = 2;
        int y = 3;

        System.out.println("Результат работы метода max = " + Math.max(x, y));

        System.out.println("Результат работы метода min = " + Math.min(x, y));

        System.out.println("Результат работы метода multiplyExact(умножение) = " + Math.multiplyExact(x, y));

    }
}
